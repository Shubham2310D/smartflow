# SmartFlow — Python 3.13 compatible requirements
# Install with:  pip install --prefer-binary -r requirements.txt

# Core data
pandas>=2.2.0
numpy>=2.1.0
scipy>=1.14.0
networkx>=3.2          # min-cost-flow roster optimiser

# Machine learning
scikit-learn>=1.5.0
xgboost>=2.1.0
joblib>=1.4.0

# Dashboard
streamlit>=1.35.0
streamlit-folium>=0.20.0
plotly>=5.22.0
folium>=0.16.0

# Explainability (SHAP waterfall on the Predict page)
shap>=0.45.0
matplotlib>=3.8.0

# Config
pyyaml>=6.0

# Optional — real-time API (not needed for the Streamlit demo):
#   pip install fastapi uvicorn        # then: uvicorn api.main:app --port 8000

# Optional — tests:
#   pip install pytest                 # then: pytest tests/

# Optional — Moran's I spatial autocorrelation (install separately if needed):
#   pip install esda libpysal

# NOT required — removed geopandas/geopy (code uses scipy for geometry)
