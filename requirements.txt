# SmartFlow — Python 3.13 compatible requirements
# Install with:  pip install --prefer-binary -r requirements.txt

# Core data
# numpy/pandas are PINNED to the versions the committed models were trained with.
# A pickled model can mis-load or predict unreliably under a different numpy, and
# an unpinned (>=) spec let the host install a newer one — the cause of the
# "model/runtime library mismatch" warning. Keep these in sync with the model
# payloads' stamped lib_versions (see model_training.lib_versions); bump all
# together and retrain.
numpy==2.2.6
pandas==2.3.2
scipy>=1.14.0
networkx>=3.2          # min-cost-flow roster optimiser

# Machine learning — PINNED for the same reason (these govern pickle/predict
# compatibility; they are exactly what check_lib_versions() guards).
scikit-learn==1.7.2
xgboost==3.1.1
joblib==1.5.2

# Dashboard
streamlit>=1.35.0
streamlit-folium>=0.20.0
plotly>=5.22.0
folium>=0.16.0

# Explainability (SHAP waterfall on the Predict page)
shap>=0.45.0
matplotlib>=3.8.0

# Config
pyyaml>=6.0

# Optional — real-time API (not needed for the Streamlit demo):
#   pip install fastapi uvicorn        # then: uvicorn api.main:app --port 8000

# Optional — tests:
#   pip install pytest                 # then: pytest tests/

# Optional — Moran's I spatial autocorrelation (install separately if needed):
#   pip install esda libpysal

# NOT required — removed geopandas/geopy (code uses scipy for geometry)
